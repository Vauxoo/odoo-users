sudo: required

language: python

python:
  - "2.7"

env:
  - VERSION="8.0" ODOO_REPO="vauxoo/odoo" INCLUDE="merge_editing" LINT_CHECK="0"

virtualenv:
  system_site_packages: true

install:
  - travis_install_nightly

script:
  - travis_run_tests ${VERSION}

after_success:
  coveralls

